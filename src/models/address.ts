export interface Address {
  address_id: string;
  customer_id: string;
  doorno: string;
  street: string;
  location: string;
  pincode: string;
  city: string;
  state: string;
  primary_use: string;
  recently_use: string;
  status?: string;
  created_date?: string;
  updated?: string;
}
