export interface Support {
  name: string;
  email: string;
  mobilenumber: string;
  location: string;
  subject: string;
  message: string;
  gofor?: string;
  contact_id?: string;
  status?: string;
  created_date?: string;
}
