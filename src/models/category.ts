export interface Category {
  category_id: string;
  cat_code: string;
  cat_name: string;
  cat_image: string;
  status: string;
  created_date: string;
  modified_date: string;
  tagline: string;
}
